---
- hosts: boxes
  become: yes
  tasks:
   - name: Install list of packages
     apt: name={{item}} state=installed
     with_items:
          - auditd
          - fail2ban
          - rkhunter
          - chkrootkit  
